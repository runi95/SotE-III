/*
import { ItemRecipe } from '../ItemRecipe';

export class LionsRing extends ItemRecipe {
    private readonly runedBracers: number = FourCC('I00S');
    private readonly improvedMoonArmor: number = FourCC('I00O');
    protected readonly recipe: number[] = [this.runedBracers, this.improvedMoonArmor];
    protected readonly resultingItem: number = FourCC('I01L');
}
*/
