import { Travel } from './Travel';

export class CenterShop extends Travel {
    protected readonly x: number = 2944;
    protected readonly y: number = -767;
    protected readonly itemTypeId: number = FourCC('I022');
}
