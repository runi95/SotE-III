import { Travel } from './Travel';

export class FelBeasts extends Travel {
    protected readonly x: number = 12000;
    protected readonly y: number = -12670;
    protected readonly itemTypeId: number = FourCC('I01W');
}
